{
  "name": "property-manager-infra",
  "version": "1.0.0",
  "description": "AWS CDK infrastructure for lotlord.app — SES inbound + outbound email",
  "scripts": {
    "cdk": "cdk",
    "deploy": "cdk deploy --all",
    "diff": "cdk diff",
    "synth": "cdk synth",
    "destroy": "cdk destroy --all"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.4.2",
    "mailparser": "^3.7.2"
  },
  "devDependencies": {
    "aws-cdk": "^2.180.0",
    "esbuild": "^0.28.0"
  }
}
